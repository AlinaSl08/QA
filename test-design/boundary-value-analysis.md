# Анализ граничных значений

**Анализ граничных значений (Boundary Value Analysis)** – это техника тестирования, при которой особое внимание уделяется минимальным и максимальным значениям диапазонов, а также значениям рядом с ними. Предполагается, что больше всего ошибок возникает именно на границах: "чуть меньше", "ровно" и "чуть больше".


**Пример (Система оценок)**: Представьте систему, которая выставляет оценку (A, B, C, D, E) в зависимости от набранных баллов.

1. Пользователь вводит числовое значение баллов в поле ввода.
2. Нажимает клавишу Enter.
3. Система анализирует введённое значение и выводит соответствующую оценку:

+ 90-100 баллов = A
+ 80-89 баллов = B
+ 70-79 баллов = C
+ 60-69 баллов = D
+ 0-59 баллов = E

Если применить технику тест-дизайна **Анализ граничных значений**, нас в первую очередь интересуют числа:

+ на границе диапазона,
+ сразу ниже границы,
+ сразу выше границы.

**Границы общего диапазона (0 и 100):**

**Нижняя граница**:

+ `-1` – невалидное значение (меньше 0)
+ `0` – валидное значение = оценка E

**Верхняя граница**:

100 – валидное значение = оценка A
101 – невалидное значение (больше 100)
 

Внутренние границы между оценками

**Граница между E и D (59 / 60)**:

+ `59` = оценка E
+ `60` = оценка D

**Граница между D и C (69 / 70)**:

+ `69` = оценка D
+ `70` = оценка C

**Граница между C и B (79 / 80)**:

+ `79` = оценка C
+ `80` = оценка B

**Граница между B и A (89 / 90)**:

+ `89` = оценка B
+ `90` = оценка A
 

**Итого, набор граничных значений для проверки может выглядеть так**:

Невалидные (вне диапазона, числовые):

+ `-1, 101`

Валидные на границах и рядом с ними:

+  `0, 59, 60, 69, 70, 79, 80, 89, 90, 100`


> 🧠 Вместо того, чтобы тестировать все 101 значение от 0 до 100 и ещё несколько невалидных, мы фокусируемся на 12 граничных значениях.
